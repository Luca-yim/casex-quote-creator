# DB integration tests run against the app's Supabase project (lsmrxbpvmvrzpbtjqygh),
# NOT the auto-generated Lovable Cloud backend.
VITE_APP_SUPABASE_URL=https://lsmrxbpvmvrzpbtjqygh.supabase.co
VITE_APP_SUPABASE_PUBLISHABLE_KEY=sb_publishable_62ymAUlxHuAoBKnMwzgSzA_aIx5YR41

# Sessions are minted with the Auth Admin API (generateLink + verifyOtp),
# exactly like e2e/scripts/mint-session.ts — passwords are no longer used and
# Turnstile is never touched. Put the APP project's service-role key in
# .env.test.local (never commit it):
#   E2E_SUPABASE_SERVICE_ROLE_KEY=...
# Override these in .env.test.local with your real persona accounts.
TEST_USER_EXTERNAL_EMAIL=external@test.local
TEST_USER_REP_EMAIL=rep@test.local
TEST_USER_ESTIMATOR_EMAIL=estimator@test.local
TEST_USER_ADMIN_EMAIL=admin@test.local

# Cloudflare always-passes Turnstile TEST site key (public, non-secret).
VITE_APP_TURNSTILE_SITE_KEY=0x4AAAAAAEfi_SCZy4f9X76o
